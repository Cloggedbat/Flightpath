{
  "versionCode": 12,
  "versionName": "0.1.11",
  "notes": "Capture works. The HERO9 records even though the shutter command errors, and its HTTP server goes silent while recording, so the app now records the full window and confirms by the new clip rather than by the camera's reply. It keeps sending stop until the camera is idle, so it is never left recording. The camera test also reports whether the preview is really MPEG-TS."
}