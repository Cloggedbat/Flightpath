{
  "versionCode": 11,
  "versionName": "0.1.10",
  "notes": "The shutter now works on a HERO9 whose control server goes silent while recording: the app sends the command, confirms from the camera's state, and keeps sending stop until the camera is really idle, so it can never again leave the camera recording with an unplayable clip. Errors name the exact URL and port that failed. From 0.1.9: legacy commands go to port 80."
}